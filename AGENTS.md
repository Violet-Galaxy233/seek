# AGENTS.md

## 项目目标

构建 Seek：一个帮助用户根据不完整、含糊甚至互相矛盾的记忆找回音乐的 AI Agent。用户可能记得封面、语言、曲风、年代、艺人地区、人声或收听场景，也可能把两个作品的特征混在一起。系统必须理解这种不确定性，而不是把每条记忆都当成硬筛选条件。

参赛目标是 2026 微信小程序开发大赛。具体产品、前端、数据、比赛和交接信息放在 `docs/`，不要把长篇设计塞进本文件。

## 当前范围

- 专辑、发行版本、曲目、艺人、语言和曲风元数据
- 专辑与单曲封面向量和结构化视觉信息
- 中文自然语言模糊记忆搜索
- 保留原始记忆并解析带置信度的线索
- 多路召回、候选解释和交互式排除
- “是 / 不是 / 不确定”的主动追问
- 微信小程序客户端和独立 Python 搜索后端

当前不要加入完整音频库、哼唱识别、多 Agent 编排或自动微调。音频分析、账号体系、播放能力和商业化只能在明确批准后进入范围。

## 架构约定

- Python 后端，FastAPI 提供协议边界。
- SQLite 用于本地原型；面向服务器的数据存储必须通过仓库接口替换。
- FAISS 用于向量索引，索引应可版本化、原子替换和回滚。
- 默认模型为本地 Qwen3-VL-Embedding-2B，模型必须通过适配器接入。
- 当前 Gradio 仅用于后端验证；参赛客户端采用微信小程序原生技术体系。
- 封面向量离线生成；线上查询只生成用户文本向量并搜索已有索引。
- 业务逻辑不得直接依赖具体数据库、模型、对象存储或云厂商 SDK。

## 常用命令

- 安装：`uv sync`
- 测试：`uv run pytest`
- 检查：`uv run ruff check .`
- 启动：`uv run python -m app`
- 下载模型：`uv run python -m app.download_models --model qwen`
- 小规模导入：`uv run python -m app.import_music --limit 100`

## 工程规则

- 不提交模型权重、下载封面、数据库、生成索引、密钥或用户数据。
- 新增生产依赖前说明用途和替代方案。
- 原始用户记忆与规范化线索必须分开保存。
- 低置信度线索只能影响排序，不得直接排除候选。
- “不确定”不得解释为“不是”。
- 每个结果必须给出匹配线索与冲突线索。
- 缺少证据时允许返回“答案可能不在曲库中”，不得虚构结果。
- 艺人关联地区、发行国家、标题语言和演唱语言是不同字段，不得互相推断替代。
- “纯音乐”是音乐属性，不是曲风。
- 同一专辑的不同发行版和封面必须能归并到共同的发行组。
- 导入数据必须保留来源、许可、抓取时间和实体标识，支持下架。
- 禁止非法抓取或提交无明确来源的版权素材。
- 行为变化必须同步更新测试和 `docs/`。

## 完成标准

一项修改只有在满足以下条件时才算完成：

- 相关测试与现有测试通过。
- 实际用户流程得到验证。
- 加载、空结果、模型缺失和网络失败得到处理。
- 数据来源和许可影响已说明。
- 行为变化对应文档已更新。
- 最终报告列出修改文件、测试结果和剩余限制。

